spring:
  application:
    name: nk-order-process-ms
  data:
    mongodb:
      host: localhost
      port: 27017
      database: admin
      username: ${MONGO_INITDB_ROOT_USERNAME}
      password: ${MONGO_INITDB_ROOT_PASSWORD}
